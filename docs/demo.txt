:root {
  /* Glassmorphic Monochrome & Crisp Accents */
  --bg-deep: #07080b;
  --glass-surface: rgba(18, 20, 29, 0.65);
  --glass-surface-hover: rgba(28, 31, 45, 0.75);
  --glass-border: rgba(255, 255, 255, 0.1);
  --glass-border-focus: rgba(255, 255, 255, 0.35);
  
  --text-white: #ffffff;
  --text-muted: #a1a1aa;
  --text-dim: #71717a;
  
  --btn-white: #ffffff;
  --btn-white-hover: #e4e4e7;
  --btn-text: #09090b;
}

* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
  font-family: 'Plus Jakarta Sans', sans-serif;
}

body {
  background-color: #07080b;
  background-image: 
    radial-gradient(at 15% 15%, rgba(56, 189, 248, 0.12) 0px, transparent 50%),
    radial-gradient(at 85% 20%, rgba(168, 85, 247, 0.12) 0px, transparent 50%),
    radial-gradient(at 50% 80%, rgba(249, 115, 22, 0.08) 0px, transparent 50%);
  background-attachment: fixed;
  color: var(--text-white);
  min-height: 100vh;
  line-height: 1.6;
}

/* =========================================
   1. DASHBOARD / INDEX STYLES
   ========================================= */
.container {
  max-width: 1100px;
  margin: 0 auto;
  padding: 2rem 1.5rem;
}

header {
  text-align: center;
  padding: 4.5rem 1rem 3rem;
}

header h1 {
  font-size: 2.8rem;
  font-weight: 700;
  letter-spacing: -0.03em;
  color: var(--text-white);
}

header h1 span {
  background: linear-gradient(135deg, #38bdf8 0%, #a855f7 50%, #f97316 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
}

header p {
  font-size: 1.05rem;
  color: var(--text-muted);
  margin-top: 0.75rem;
  max-width: 620px;
  margin-left: auto;
  margin-right: auto;
}

.hero-actions {
  display: flex;
  justify-content: center;
  gap: 1rem;
  margin-top: 2.5rem;
}

.features-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
  gap: 1.5rem;
  margin-top: 4rem;
}

.feature-card {
  background: var(--glass-surface);
  backdrop-filter: blur(16px);
  -webkit-backdrop-filter: blur(16px);
  border: 1px solid var(--glass-border);
  border-radius: 16px;
  padding: 2.2rem;
  transition: all 0.25s ease;
}

.feature-card:hover {
  background: var(--glass-surface-hover);
  border-color: var(--glass-border-focus);
  transform: translateY(-3px);
}

.feature-card h3 {
  font-size: 1.2rem;
  margin-bottom: 0.6rem;
  font-weight: 600;
  color: var(--text-white);
}

.feature-card p {
  color: var(--text-muted);
  font-size: 0.92rem;
  line-height: 1.55;
}

/* =========================================
   2. BUTTONS (GLOBAL)
   ========================================= */
.btn-primary {
  background: linear-gradient(135deg, #38bdf8 0%, #818cf8 100%);
  color: #07080b;
  padding: 0.9rem 2rem;
  font-size: 0.95rem;
  font-weight: 700;
  border-radius: 10px;
  border: none;
  text-decoration: none;
  cursor: pointer;
  display: inline-block;
  transition: all 0.25s ease;
  box-shadow: 0 4px 20px rgba(56, 189, 248, 0.25);
}

.btn-primary:hover {
  transform: translateY(-2px);
  box-shadow: 0 6px 25px rgba(56, 189, 248, 0.45);
}

.btn-secondary {
  background: rgba(255, 255, 255, 0.05);
  color: var(--text-muted);
  border: 1px solid var(--glass-border);
  backdrop-filter: blur(10px);
  -webkit-backdrop-filter: blur(10px);
  padding: 0.85rem 1.5rem;
  border-radius: 10px;
  font-weight: 500;
  cursor: pointer;
  transition: all 0.2s ease;
}

.btn-secondary:hover {
  background: rgba(255, 255, 255, 0.1);
  color: var(--text-white);
  border-color: var(--glass-border-focus);
}

.full-width {
  width: 100%;
  margin-top: 0.75rem;
}

.btn-row {
  display: flex;
  gap: 1rem;
  margin-top: 1.25rem;
}

/* =========================================
   3. ASSESSMENT LAYOUT
   ========================================= */
.assessment-wrapper {
  display: grid;
  grid-template-columns: 1fr 1.35fr;
  min-height: 100vh;
}

/* Left Sidebar (Vibrant & Illustration Ready) */
.carousel-pane {
  background: radial-gradient(circle at 20% 30%, #1a1c2e 0%, #0d0e17 100%);
  border-right: 1px solid var(--glass-border);
  padding: 3.5rem;
  display: flex;
  flex-direction: column;
  justify-content: space-between;
  position: relative;
}

.brand-badge span {
  font-size: 0.85rem;
  font-weight: 600;
  color: #38bdf8;
  background: rgba(56, 189, 248, 0.12);
  padding: 0.4rem 0.9rem;
  border-radius: 20px;
  border: 1px solid rgba(56, 189, 248, 0.3);
}

.slider-container {
  min-height: 220px;
}

.slide {
  display: none;
  opacity: 0;
  transition: opacity 0.4s ease-in-out;
}

.slide.active {
  display: block;
  opacity: 1;
}

.slide-tag {
  color: #fb923c;
  font-size: 0.8rem;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.05em;
  margin-bottom: 0.75rem;
}

.slide h2 {
  font-size: 1.85rem;
  font-weight: 700;
  line-height: 1.35;
  margin-bottom: 1rem;
}

.slide p {
  color: var(--text-muted);
  font-size: 0.95rem;
  line-height: 1.6;
}

.slider-dots {
  display: flex;
  gap: 0.5rem;
}

.dot {
  width: 24px;
  height: 4px;
  background: rgba(255, 255, 255, 0.15);
  border-radius: 2px;
  cursor: pointer;
  transition: background 0.3s ease;
}

.dot.active {
  background: #ffffff;
}

/* =========================================
   4. RIGHT PANE (GLASSMORPHIC BLACK & WHITE)
   ========================================= */
.form-pane {
  padding: 3.5rem 4rem;
  display: flex;
  flex-direction: column;
  justify-content: center;
  max-width: 680px;
  background: radial-gradient(circle at top right, rgba(255, 255, 255, 0.03) 0%, transparent 70%);
}

.form-header {
  margin-bottom: 2rem;
}

.step-indicator {
  display: flex;
  flex-direction: column;
  gap: 0.6rem;
}

#step-label {
  font-size: 0.85rem;
  font-weight: 600;
  letter-spacing: 0.02em;
  color: var(--text-muted);
  text-transform: uppercase;
}

.progress-track {
  width: 100%;
  height: 4px;
  background: rgba(255, 255, 255, 0.08);
  border-radius: 2px;
  overflow: hidden;
}

.progress-bar {
  width: 50%;
  height: 100%;
  background: #ffffff;
  box-shadow: 0 0 10px rgba(255, 255, 255, 0.5);
  transition: width 0.3s ease;
}

.form-step {
  display: none;
}

.form-step.active-step {
  display: block;
}

.form-step h3 {
  font-size: 1.7rem;
  font-weight: 700;
  color: var(--text-white);
  letter-spacing: -0.02em;
  margin-bottom: 0.3rem;
}

.form-step .subtitle {
  color: var(--text-dim);
  font-size: 0.9rem;
  margin-bottom: 1.75rem;
}

.grid-2 {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 1.1rem;
}

.input-group {
  display: flex;
  flex-direction: column;
  gap: 0.45rem;
  margin-bottom: 1.3rem;
}

.input-group label {
  font-size: 0.82rem;
  font-weight: 500;
  color: var(--text-muted);
  letter-spacing: 0.01em;
}

/* Glassmorphic Form Controls */
.input-group input,
.input-group select {
  background: rgba(255, 255, 255, 0.04);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  border: 1px solid var(--glass-border);
  color: var(--text-white);
  padding: 0.8rem 1rem;
  border-radius: 10px;
  font-size: 0.9rem;
  outline: none;
  transition: all 0.2s ease;
}

.input-group input:focus,
.input-group select:focus {
  background: rgba(255, 255, 255, 0.07);
  border-color: var(--glass-border-focus);
  box-shadow: 0 0 0 3px rgba(255, 255, 255, 0.06);
}

.input-group select option {
  background-color: #12141c;
  color: #ffffff;
}

.input-group input::placeholder {
  color: #52525b;
}

/* Phone Field Container */
.phone-group {
  display: flex;
  gap: 0.5rem;
}

.phone-group select {
  width: 115px;
  flex-shrink: 0;
}

.phone-group input {
  flex-grow: 1;
}

/* =========================================
   5. RESPONSIVE QUERIES
   ========================================= */
@media (max-width: 900px) {
  .assessment-wrapper { 
    grid-template-columns: 1fr; 
  }
  .carousel-pane, .form-pane { 
    padding: 2rem; 
  }
  .grid-2 { 
    grid-template-columns: 1fr; 
  }
}

/* =========================================
   6. REPORT DASHBOARD STYLES (ENHANCED)
   ========================================= */
.report-container {
  max-width: 1150px;
  margin: 0 auto;
  padding: 3rem 1.5rem 5rem;
}

.report-header {
  text-align: left;
  padding: 1.5rem 0 2.5rem;
  border-bottom: 1px solid var(--glass-border, rgba(255, 255, 255, 0.1));
  margin-bottom: 2.5rem;
  position: relative;
}

.report-header h1 {
  font-size: 2.5rem;
  font-weight: 800;
  margin-top: 0.5rem;
  background: linear-gradient(135deg, #ffffff 30%, #38bdf8 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  letter-spacing: -0.5px;
}

.profile-summary {
  color: #94a3b8;
  margin-top: 0.75rem;
  font-size: 1.05rem;
  line-height: 1.65;
  max-width: 850px;
}

.report-section {
  margin-bottom: 3.5rem;
}

.section-title {
  font-size: 1.45rem;
  font-weight: 700;
  margin-bottom: 1.5rem;
  color: #f8fafc;
  display: flex;
  align-items: center;
  gap: 0.6rem;
}

.section-title::before {
  content: "";
  display: inline-block;
  width: 4px;
  height: 1.25rem;
  background: #38bdf8;
  border-radius: 4px;
}

/* Glassmorphism Card Enhancements */
.glass-card {
  background: rgba(255, 255, 255, 0.03);
  backdrop-filter: blur(16px);
  -webkit-backdrop-filter: blur(16px);
  border: 1px solid rgba(255, 255, 255, 0.08);
  border-radius: 16px;
  padding: 1.75rem;
  box-shadow: 0 4px 20px -2px rgba(0, 0, 0, 0.5);
  transition: transform 0.25s ease, border-color 0.25s ease, box-shadow 0.25s ease;
}

.glass-card:hover {
  transform: translateY(-4px);
  border-color: rgba(255, 255, 255, 0.18);
  box-shadow: 0 12px 30px -4px rgba(0, 0, 0, 0.6);
}

/* Dynamic Grid Handling (Handles 3, 4, 5, or 6 cards cleanly) */
.cards-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
  gap: 1.5rem;
}

/* Smart span for 5-card scenarios on wider screens */
@media (min-width: 1024px) {
  .cards-grid:has(> :last-child:nth-child(5)) > :nth-child(4) {
    grid-column: span 1;
  }
}

/* Career Card Accents & Colors */
.career-card {
  position: relative;
  overflow: hidden;
  border-left: 4px solid #38bdf8;
  background: linear-gradient(180deg, rgba(255, 255, 255, 0.04) 0%, rgba(255, 255, 255, 0.01) 100%);
}

/* Color variations for distinct cards */
.cards-grid > .career-card:nth-child(1) { border-left-color: #38bdf8; } /* Cyan */
.cards-grid > .career-card:nth-child(2) { border-left-color: #a855f7; } /* Purple */
.cards-grid > .career-card:nth-child(3) { border-left-color: #34d399; } /* Emerald */
.cards-grid > .career-card:nth-child(4) { border-left-color: #fbbf24; } /* Amber */
.cards-grid > .career-card:nth-child(5) { border-left-color: #f43f5e; } /* Rose */

.career-card h3 {
  font-size: 1.25rem;
  font-weight: 600;
  color: #ffffff;
  margin-bottom: 0.6rem;
}

.career-card p {
  font-size: 0.92rem;
  color: #94a3b8;
  line-height: 1.6;
  margin-bottom: 0.85rem;
}

.career-card .tag-label {
  font-size: 0.72rem;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.05em;
  color: #38bdf8;
  background: rgba(56, 189, 248, 0.1);
  padding: 0.25rem 0.5rem;
  border-radius: 4px;
  display: inline-block;
  margin-top: 0.5rem;
}

/* Dual Column Layout (Skills & Degrees) */
.dual-section-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 1.5rem;
  margin-bottom: 3.5rem;
}

@media (max-width: 768px) {
  .dual-section-grid,
  .roadmap-grid {
    grid-template-columns: 1fr;
  }
}

/* Roadmap Grid & Badges */
.roadmap-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 1.5rem;
}

.roadmap-badge {
  display: inline-block;
  font-size: 0.75rem;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.05em;
  padding: 0.35rem 0.75rem;
  border-radius: 9999px;
  margin-bottom: 1rem;
}

.roadmap-badge.short-term {
  background: rgba(56, 189, 248, 0.15);
  color: #38bdf8;
  border: 1px solid rgba(56, 189, 248, 0.3);
}

.roadmap-badge.long-term {
  background: rgba(251, 146, 60, 0.15);
  color: #fb923c;
  border: 1px solid rgba(251, 146, 60, 0.3);
}

/* Verdict Callout Banner */
.verdict-card {
  text-align: center;
  padding: 3rem 2rem;
  background: radial-gradient(circle at center, rgba(56, 189, 248, 0.08) 0%, rgba(255, 255, 255, 0.02) 100%);
  border: 1px solid rgba(56, 189, 248, 0.25);
}

.verdict-card h3 {
  font-size: 1.5rem;
  font-weight: 700;
  color: #ffffff;
  margin-bottom: 0.85rem;
}

.verdict-card p {
  color: #cbd5e1;
  font-size: 1rem;
  max-width: 750px;
  margin: 0 auto 2rem;
  line-height: 1.7;
}

.report-actions {
  display: flex;
  justify-content: center;
  gap: 1.25rem;
}

/* Loading Overlay */
.loading-overlay {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  min-height: 80vh;
  text-align: center;
  padding: 2rem;
}

.spinner {
  width: 48px;
  height: 48px;
  border: 3px solid rgba(255, 255, 255, 0.1);
  border-top-color: #ffffff;
  border-radius: 50%;
  animation: spin 0.8s linear infinite;
  margin-bottom: 1.5rem;
}

@keyframes spin {
  to { transform: rotate(360deg); }
}

@media (max-width: 800px) {
  .dual-section-grid, .roadmap-grid {
    grid-template-columns: 1fr;
  }
}

/* Card Individual Border Accents & Header Colors */
.features-grid .feature-card:nth-child(1) {
  border-top: 2px solid rgba(56, 189, 248, 0.6);
}
.features-grid .feature-card:nth-child(2) {
  border-top: 2px solid rgba(168, 85, 247, 0.6);
}
.features-grid .feature-card:nth-child(3) {
  border-top: 2px solid rgba(249, 115, 22, 0.6);
}

.features-grid .feature-card:nth-child(1) h3 { color: #38bdf8; }
.features-grid .feature-card:nth-child(2) h3 { color: #c084fc; }
.features-grid .feature-card:nth-child(3) h3 { color: #fb923c; }

/* Comparison Grid for Dashboard */
.comparison-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 1.5rem;
}


/* =========================================
   INTERACTIVE LOADER & GAME/TRIVIA CARD
   ========================================= */
.loading-backdrop {
  position: fixed;
  inset: 0;
  background: rgba(10, 15, 29, 0.88);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  display: flex;
  justify-content: center;
  align-items: center;
  z-index: 9999;
  padding: 1.5rem;
}

.loading-card {
  max-width: 480px;
  width: 100%;
  text-align: center;
  padding: 2.5rem 2rem;
  border: 1px solid rgba(56, 189, 248, 0.25);
  box-shadow: 0 20px 50px rgba(0, 0, 0, 0.6), 0 0 30px rgba(56, 189, 248, 0.1);
}

.loader-spinner-wrapper {
  position: relative;
  width: 72px;
  height: 72px;
  margin: 0 auto 1.5rem;
  display: flex;
  align-items: center;
  justify-content: center;
}

.pulsing-orbit {
  position: absolute;
  inset: 0;
  border-radius: 50%;
  border: 2px solid transparent;
  border-top-color: #38bdf8;
  border-right-color: #a855f7;
  animation: spinOrbit 1.2s linear infinite;
}

.loader-icon {
  font-size: 1.8rem;
  animation: pulseIcon 1.5s ease-in-out infinite alternate;
}

@keyframes spinOrbit {
  to { transform: rotate(360deg); }
}

@keyframes pulseIcon {
  from { transform: scale(0.9); opacity: 0.8; }
  to { transform: scale(1.1); opacity: 1; }
}

#loading-title {
  font-size: 1.4rem;
  font-weight: 700;
  color: #f8fafc;
  margin-bottom: 0.4rem;
}

.loading-subtitle {
  font-size: 0.92rem;
  color: #94a3b8;
  min-height: 1.4rem;
  margin-bottom: 1.5rem;
}

.progress-track {
  width: 100%;
  height: 8px;
  background: rgba(255, 255, 255, 0.08);
  border-radius: 9999px;
  overflow: hidden;
  margin-bottom: 0.75rem;
}

.progress-fill {
  height: 100%;
  width: 15%;
  background: linear-gradient(90deg, #38bdf8, #818cf8, #a855f7);
  border-radius: 9999px;
  transition: width 0.6s cubic-bezier(0.4, 0, 0.2, 1);
}

.loading-metrics {
  display: flex;
  justify-content: space-between;
  font-size: 0.78rem;
  color: #64748b;
  margin-bottom: 1.5rem;
  font-weight: 600;
}

.loading-fact-box {
  background: rgba(255, 255, 255, 0.03);
  border: 1px solid rgba(255, 255, 255, 0.06);
  border-radius: 10px;
  padding: 1rem;
  text-align: left;
}

.fact-tag {
  display: inline-block;
  font-size: 0.72rem;
  font-weight: 700;
  color: #38bdf8;
  text-transform: uppercase;
  letter-spacing: 0.05em;
  margin-bottom: 0.35rem;
}

#dynamic-fact {
  font-size: 0.85rem;
  color: #cbd5e1;
  line-height: 1.5;
  margin: 0;
}


@media (max-width: 800px) {
  .comparison-grid {
    grid-template-columns: 1fr;
  }
}

/* =========================================
   TOP NAVIGATION & FOOTER BAR
   ========================================= */
.top-nav {
  position: sticky;
  top: 0;
  z-index: 100;
  background: rgba(11, 17, 32, 0.85);
  backdrop-filter: blur(16px);
  -webkit-backdrop-filter: blur(16px);
  border-bottom: 1px solid var(--glass-border);
  padding: 1rem 1.5rem;
}

.nav-container {
  max-width: 1100px;
  margin: 0 auto;
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.nav-brand {
  display: flex;
  align-items: center;
  gap: 0.5rem;
  font-weight: 700;
  font-size: 1.15rem;
  color: var(--text-white);
}

.brand-icon {
  font-size: 1.3rem;
}

.nav-links {
  display: flex;
  align-items: center;
  gap: 1.75rem;
}

.nav-links a {
  color: var(--text-muted);
  text-decoration: none;
  font-size: 0.9rem;
  font-weight: 500;
  transition: color 0.2s ease;
}

.nav-links a:hover {
  color: var(--text-white);
}

.btn-nav {
  background: rgba(255, 255, 255, 0.1);
  border: 1px solid var(--glass-border);
  padding: 0.5rem 1rem !important;
  border-radius: 8px;
  color: var(--text-white) !important;
  font-weight: 600 !important;
  transition: all 0.2s ease;
}

.btn-nav:hover {
  background: var(--text-white);
  color: #0b1120 !important;
}

/* Footer Bar */
.footer-bar {
  margin-top: 5rem;
  border-top: 1px solid var(--glass-border);
  background: rgba(7, 8, 11, 0.95);
  padding: 2rem 1.5rem;
  color: var(--text-muted);
  font-size: 0.85rem;
}

.footer-container {
  max-width: 1100px;
  margin: 0 auto;
  display: flex;
  justify-content: space-between;
  align-items: center;
  flex-wrap: wrap;
  gap: 1rem;
}

.footer-links {
  display: flex;
  gap: 1.5rem;
}

.footer-links a {
  color: var(--text-muted);
  text-decoration: none;
  transition: color 0.2s ease;
}

.footer-links a:hover {
  color: var(--text-white);
}

@media (max-width: 768px) {
  .nav-links {
    display: none; /* Keeps mobile view clean */
  }
}  

